# StoreManagementApp Admin Web

Next.js App Router administration application.

```bash
npm install
npm run dev:web
```

Open `http://localhost:3000`.

The home page checks `http://localhost:3001/api/v1/health` and reports whether
the NestJS API is reachable.
